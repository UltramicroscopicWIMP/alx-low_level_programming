#include<stdio.h>
/**
 *main - prints to standard output a string text with newline
 *
 *Return: int. returns 0 before exiting
 */
int main(void)
{
printf("with proper grammar, but the outcome is a piece of art,\n");
return (0);
}
