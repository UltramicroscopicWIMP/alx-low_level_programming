#include <stdio.h>

/**
 *main - prints the alphabet in lower case
 *
 *Return: int. returns 0 on exit
 */
int main(void)
{
  char c = 'a';
  do
    {
      putchar(c);
      ++c;
    }
  while(c <= 'z');
  putchar('\n');
  return (0);
}
